## Summary

What does this change and why?

## How I verified it

<!-- e.g. `npm run build && npm run lint`, plus manual `surgex check` output -->

## Checklist

- [ ] `npm run build` passes
- [ ] `npm run lint` passes
- [ ] `npm run format:check` passes
- [ ] Updated `CHANGELOG.md` under `[Unreleased]` if user-facing
